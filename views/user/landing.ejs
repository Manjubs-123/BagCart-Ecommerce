<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BagHub | Premium Backpacks & Bags</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Swiper.js CSS & JS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

    <!-- Axios -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
      :root {
        --primary: #1a56db;
        --primary-dark: #1e429f;
        --secondary: #047857;
        --accent: #f59e0b;
        --light: #f8fafc;
        --dark: #1e293b;
        --gray: #64748b;
      }
      
      body {
        font-family: 'Inter', system-ui, -apple-system, sans-serif;
        color: var(--dark);
      }
      
      .swiper-button-next,
      .swiper-button-prev {
        color: var(--primary);
        background-color: white;
        width: 44px;
        height: 44px;
        border-radius: 50%;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      }
      
      .swiper-button-next:after,
      .swiper-button-prev:after {
        font-size: 18px;
      }
      
      .swiper-pagination-bullet-active {
        background-color: var(--primary);
      }
      
      .gradient-bg {
        background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      }
      
      .card-hover {
        transition: all 0.3s ease;
      }
      
      .card-hover:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      }
      
      .section-title {
        position: relative;
        display: inline-block;
        margin-bottom: 2.5rem;
      }
      
      .section-title:after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 0;
        width: 60%;
        height: 4px;
        background: var(--primary);
        border-radius: 2px;
      }
      
      .center-title:after {
        left: 20%;
      }
      
      .btn-primary {
        background-color: var(--primary);
        transition: all 0.3s ease;
      }
      
      .btn-primary:hover {
        background-color: var(--primary-dark);
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
      }
      
      .btn-secondary {
        background-color: var(--secondary);
        transition: all 0.3s ease;
      }
      
      .btn-secondary:hover {
        background-color: #065f46;
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
      }
      
      .badge {
        position: absolute;
        top: 12px;
        left: 12px;
        background-color: var(--accent);
        color: white;
        font-size: 0.75rem;
        font-weight: 600;
        padding: 0.25rem 0.75rem;
        border-radius: 9999px;
        z-index: 10;
      }
      
      .discount-badge {
        background-color: #ef4444;
      }
      
      .new-badge {
        background-color: var(--primary);
      }
      
      .trending-badge {
        background-color: var(--secondary);
      }
    </style>
  </head>

  <body class="bg-gray-50 text-gray-900">
    <!-- HEADER -->
  <%- include('../partials/landingheader') %>

    <!-- HERO SECTION -->
    <section class="gradient-bg text-white">
      <div class="max-w-7xl mx-auto flex flex-col-reverse md:flex-row items-center justify-between px-6 py-16 gap-10">
        <!-- Text Section -->
        <div class="max-w-lg text-center md:text-left">
          <h1 class="text-4xl md:text-5xl font-extrabold leading-tight mb-4">
            Adventure Awaits with <br />
            <span class="text-yellow-300">Premium Backpacks</span>
          </h1>
          <p class="text-blue-100 mb-6 text-lg leading-relaxed">
            Discover our collection of durable, stylish, and functional
            backpacks designed for every journey — from urban commutes to
            mountain adventures.
          </p>
          <div class="flex flex-col sm:flex-row gap-4">
            <a
              href="/user/shop"
              class="btn-primary text-white px-8 py-3 rounded-lg font-semibold inline-block shadow-lg"
              >Explore Collection</a
            >
            <a
              href="/user/shop"
              class="bg-white text-blue-700 px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition inline-block shadow-lg"
              >Shop Sale</a
            >
          </div>
        </div>

          <!-- Image Section -->
        <div class="flex justify-center md:justify-end relative">
          <div class="relative">
            <img
              src="https://res.cloudinary.com/db5uwjwdv/image/upload/v1762505624/619uBH8R88L._AC_UL480_FMwebp_QL65__1_jhq7eo.png"
              alt="Premium backpack"
              class="w-64 md:w-96 rounded-2xl shadow-2xl transform hover:scale-105 transition duration-500"
            />
        
          </div>
        </div>
      </div>
    </section>

    <!-- FEATURED PRODUCTS -->
<section id="featured" class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4">
    <h2 class="section-title center-title text-3xl font-bold text-center text-gray-900">
      Featured Products
    </h2>

    <div class="swiper featuredSwiper relative">
      <div class="swiper-wrapper pb-10">

        <% if (featuredProducts && featuredProducts.length > 0) { %>
          <% featuredProducts.forEach(product => { %>
            <div class="swiper-slide">
              <div class="card-hover bg-white border border-gray-200 rounded-xl p-4 h-full flex flex-col">
                <div class="relative">
                  <img
                    src="<%= product.variants?.[0]?.images?.[0]?.url || '/images/no-image.jpg' %>"
                    alt="<%= product.name %>"
                    class="rounded-lg w-full h-64 object-cover mb-4"
                  />
                  <span class="badge new-badge">NEW</span>
                </div>
                <h3 class="font-bold text-lg mb-2 text-gray-900"><%= product.name %></h3>
                <div class="flex items-center mb-4">
                  <p class="text-gray-900 font-bold text-xl">₹<%= product.variants?.[0]?.price || 0 %></p>
                </div>
                <div class="mt-auto flex space-x-2">
                  <button class="btn-primary text-white px-4 py-2 rounded-lg text-sm font-medium flex-1">
                    Add to Cart
                  </button>
                  <button class="border border-gray-300 text-gray-700 px-4 py-2 rounded-lg text-sm font-medium hover:bg-gray-50 transition">
                    <i class="far fa-heart"></i>
                  </button>
                </div>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <p class="text-gray-500 text-center">No featured products found</p>
        <% } %>

      </div>

      <div class="swiper-button-next mt-10"></div>
      <div class="swiper-button-prev mt-10"></div>
    </div>
  </div>
</section>


   

   <!-- SHOP YOUR FAVOURITES -->
<section class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4">
    <h2 class="section-title text-3xl font-bold text-gray-900">
      Shop Your Favourites
    </h2>

    <div class="swiper favSwiper mt-8">
      <div class="swiper-wrapper pb-10">
        <% if (favouriteProducts && favouriteProducts.length > 0) { %>
          <% favouriteProducts.forEach(product => { %>
            <div class="swiper-slide">
              <div class="card-hover bg-white border border-gray-200 rounded-xl p-4 h-full flex flex-col">
                <div class="relative">
                  <img
                    src="<%= product.variants?.[0]?.images?.[0]?.url || '/images/no-image.jpg' %>"
                    alt="<%= product.name %>"
                    class="rounded-lg w-full h-64 object-cover mb-4"
                  />
                  <span class="badge">Fav</span>
                </div>
                <h3 class="font-bold text-lg mb-2 text-gray-900"><%= product.name %></h3>
                <p class="text-gray-700 mb-3"><%= product.category?.name || 'Uncategorized' %></p>
                <p class="text-gray-900 font-bold text-xl mb-4">₹<%= product.variants?.[0]?.price || 0 %></p>
                <button class="btn-primary text-white px-4 py-2 rounded-lg text-sm font-medium">
                  Add to Cart
                </button>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <p class="text-gray-500 text-center">No favourite products available</p>
        <% } %>
      </div>
      <div class="swiper-pagination mt-4"></div>
    </div>
  </div>
</section>


    <!-- HAND PICKED -->
<section class="py-16 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4">
    <h2 class="section-title center-title text-3xl font-bold text-center text-gray-900">
      Hand Picked Collection
    </h2>
    <p class="text-center text-gray-600 max-w-2xl mx-auto mb-10">
      Discover our hand-picked collection featuring quality, style, and timeless appeal.
    </p>

    <div class="swiper handSwiper">
      <div class="swiper-wrapper pb-10">
        <% if (handpickedProducts && handpickedProducts.length > 0) { %>
          <% handpickedProducts.forEach(product => { %>
            <div class="swiper-slide">
              <div class="relative group overflow-hidden rounded-xl">
                <img
                  src="<%= product.variants?.[0]?.images?.[0]?.url || '/images/no-image.jpg' %>"
                  class="rounded-xl w-full h-72 object-cover transition duration-500 group-hover:scale-110"
                  alt="<%= product.name %>"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black to-transparent opacity-70 flex items-end p-6">
                  <div class="text-white">
                    <h3 class="font-bold text-lg"><%= product.name %></h3>
                    <p class="text-sm">₹<%= product.variants?.[0]?.price || 0 %></p>
                  </div>
                </div>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <p class="text-gray-500 text-center">No handpicked products available</p>
        <% } %>
      </div>
      <div class="swiper-pagination mt-4"></div>
    </div>
  </div>
</section>


    <!-- TRENDING NOW -->
<section class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4">
    <h2 class="section-title text-3xl font-bold text-gray-900">
      Trending Now
    </h2>

    <div class="swiper trendSwiper mt-8">
      <div class="swiper-wrapper pb-10">
        <% if (trendingProducts && trendingProducts.length > 0) { %>
          <% trendingProducts.forEach(product => { %>
            <div class="swiper-slide">
              <div class="relative group overflow-hidden rounded-xl">
                <img
                  src="<%= product.variants?.[0]?.images?.[0]?.url || '/images/no-image.jpg' %>"
                  class="rounded-xl w-full h-72 object-cover transition duration-500 group-hover:scale-110"
                  alt="<%= product.name %>"
                />
                <div class="absolute top-4 right-4 bg-white rounded-full w-10 h-10 flex items-center justify-center shadow-md">
                  <i class="fas fa-fire text-orange-500"></i>
                </div>
              </div>
              <h3 class="font-bold text-lg mt-3 text-gray-900"><%= product.name %></h3>
              <p class="text-gray-700">₹<%= product.variants?.[0]?.price || 0 %></p>
            </div>
          <% }); %>
        <% } else { %>
          <p class="text-gray-500 text-center">No trending products available</p>
        <% } %>
      </div>
      <div class="swiper-pagination mt-4"></div>
    </div>
  </div>
</section>


  

    <!-- FOOTER -->
 <%- include('../partials/footer') %>

    <!-- JS SECTION -->
    <script>
      // Swiper Configurations
      const config = {
        slidesPerView: 1,
        spaceBetween: 20,
        loop: true,
        autoplay: { delay: 3000, disableOnInteraction: false },
        pagination: { el: ".swiper-pagination", clickable: true },
        breakpoints: {
          640: { slidesPerView: 1.2 },
          768: { slidesPerView: 2 },
          1024: { slidesPerView: 3 },
          1280: { slidesPerView: 4 },
        },
      };

      const featuredConfig = {
        ...config,
        navigation: { nextEl: ".swiper-button-next", prevEl: ".swiper-button-prev" },
      };

      new Swiper(".featuredSwiper", featuredConfig);
      new Swiper(".favSwiper", config);
      new Swiper(".handSwiper", config);
      new Swiper(".trendSwiper", config);

      // Axios Demo
      async function loadProducts() {
        try {
          const res = await axios.get("/api/products");
          console.log("Products:", res.data);
        } catch (err) {
          console.error("Error loading products:", err);
        }
      }
      loadProducts();
      
      // Add to cart animation
      document.addEventListener('DOMContentLoaded', function() {
        const addToCartButtons = document.querySelectorAll('.btn-primary');
        
        addToCartButtons.forEach(button => {
          button.addEventListener('click', function() {
            const originalText = this.innerHTML;
            this.innerHTML = '<i class="fas fa-check mr-2"></i> Added to Cart';
            this.classList.add('bg-green-600');
            
            setTimeout(() => {
              this.innerHTML = originalText;
              this.classList.remove('bg-green-600');
            }, 2000);
          });
        });
      });
    </script>
  </body>
</html>