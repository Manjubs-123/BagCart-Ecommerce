<!-- <!DOCTYPE html>
<html>
<head>
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Add Variants</title>
</head>
<body class="bg-gray-100 flex justify-center items-center min-h-screen">
  <form action="/admin/products/<%= product._id %>/variants" method="POST" enctype="multipart/form-data"
    class="bg-white p-8 rounded-2xl shadow-lg w-full max-w-lg">
    <h2 class="text-2xl font-semibold text-center mb-6">Add Variants</h2>

    <div id="variant-container">
      <div class="variant bg-gray-50 border p-4 rounded-lg mb-4">
        <input name="variants[0][colour]" placeholder="Colour" class="border w-full p-2 mb-3 rounded-lg" required />
        <input name="variants[0][price]" type="number" placeholder="Price" class="border w-full p-2 mb-3 rounded-lg" required />
        <input name="variants[0][stock]" type="number" placeholder="Stock" class="border w-full p-2 mb-3 rounded-lg" required />
        <input name="variants[0][images]" type="file" multiple class="border w-full p-2 mb-3 rounded-lg" />
      </div>
    </div>

    <button type="button" id="addVariantBtn" class="w-full bg-gray-200 hover:bg-gray-300 p-2 rounded-lg mb-4">➕ Add Another Variant</button>

    <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white p-2 rounded-lg">
      Save Variants
    </button>
  </form>

  <script>
    let variantIndex = 1;
    document.getElementById("addVariantBtn").addEventListener("click", () => {
      const container = document.getElementById("variant-container");
      const newVariant = document.createElement("div");
      newVariant.classList = "variant bg-gray-50 border p-4 rounded-lg mb-4";

      newVariant.innerHTML = `
        <input name="variants[${variantIndex}][colour]" placeholder="Colour" class="border w-full p-2 mb-3 rounded-lg" required />
        <input name="variants[${variantIndex}][price]" type="number" placeholder="Price" class="border w-full p-2 mb-3 rounded-lg" required />
        <input name="variants[${variantIndex}][stock]" type="number" placeholder="Stock" class="border w-full p-2 mb-3 rounded-lg" required />
        <input name="variants[${variantIndex}][images]" type="file" multiple class="border w-full p-2 mb-3 rounded-lg" />
      `;
      container.appendChild(newVariant);
      variantIndex++;
    });
  </script>
</body>
</html> -->
